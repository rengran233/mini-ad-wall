# ------------------------------------------------
# Server / Backend (Koa + Prisma)
# ------------------------------------------------
# Prisma SQLite 数据库文件 (绝对不能提交)
*.db
*.db-journal
*.db-wal

# Prisma 生成的数据库文件具体路径 (双重保险)
server/prisma/*.db
server/prisma/*.db-journal
server/prisma/*.db-wal

# 后端构建产物
server/dist/

# 环境变量 (再次确认)
server/.env