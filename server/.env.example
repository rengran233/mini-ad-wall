# 服务端口
PORT=3000

# 基础 URL (用于拼接文件访问路径)
BASE_URL=http://localhost:3000

# 上传文件存储目录 (相对于项目根目录)
UPLOAD_DIR=uploads
TEMP_UPLOAD_DIR=temp_uploads

# 数据库连接 (Prisma 已经在使用，这里统一管理)
DATABASE_URL="file:./dev.db" 