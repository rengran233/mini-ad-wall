# 开发文档

## 1\. 架构设计

### 1.1 前端设计模式

为了解决复杂业务逻辑与UI耦合的问题，项目严格遵循**View + Hook**的分离模式：

  * **UI 层 (`index.tsx` / `Component.tsx`)**: 仅负责视图渲染和样式布局，不包含业务状态逻辑。
  * **逻辑层 (`useHook.ts`)**: 封装状态（State）、副作用（Effect）和交互处理函数（Handlers），向 UI 层暴露接口。
  * **入口文件 (`index.ts`)**: 采用Barrel模式转发组件，保持引用路径简洁。

### 1.2 后端分层

  * **Router**: 定义 API 路径与 HTTP 方法。
  * **Controller**: 处理请求参数，调用 Prisma 进行数据库操作，返回响应。
  * **Prisma Client**: 自动生成的强类型数据库访问层。

## 2\. 核心逻辑实现

### 2.1 竞价排名算法

广告列表不按时间排序，而是依据“热度”和“出价”综合计算权重。

**公式**：
$$Score = Pricing + (Pricing \times Clicked \times 0.42)$$
  * `Pricing`: 广告主出价。
  * `Clicked`: 广告被点击次数。
  * `0.42`: 神秘系数。

**实现位置**: 现在的逻辑是在`adController.ts`的`getAds`方法中进行
排序逻辑目前在内存中进行`Array.sort`，适合数据量较小的场景。

### 2.2 动态表单

为了提高系统的灵活性，广告创建/编辑表单不再由前端硬编码，而是由后端完全控制。

1.  **配置定义**: 后端 `server/src/constants/formSchema.ts` 定义了 JSON Schema。

2.  **前端渲染**:
    *   前端请求 `GET /ads/schema` 获取配置。
    *   `AdModal` 组件遍历 Schema，根据 `component` 字段动态映射到 `Input`, `InputNumber` 或自定义的 `VideoUpload` 组件。
    *   **优势**: 修改表单字段（如修改 Label、增加校验、调整顺序）只需重启后端，前端无需复杂改动。

### 2.3 视频处理

#### 上传流程
1.  前端通过 `multipart/form-data` 上传文件。
2.  后端 `@koa/multer` 中间件拦截，生成UUID文件名并保存至 `server/uploads`。
3.  后端返回静态资源URL(如`http://localhost:3000/uploads/xxx.mp4`)。
4.  前端将 URL 填入表单的 `video` 字段。

#### 智能删除策略 (Reference Counting)
防止多则广告共享同一视频资源时，一条广告及对应视频的删除导致所有需要该视频的广告失去所需资源

**解决方案**:
在 `deleteAd` 控制器中实现了引用计数检查：
1.  查询数据库中所有使用该 `video` URL 的广告数量 (`count`)。
2.  **仅当 `count <= 1` 时**（即只有当前要删除的广告在使用该视频），才执行 `fs.unlinkSync` 物理删除文件。
3.  否则，仅删除数据库中的广告记录，保留物理文件供其他副本使用。

#### 多视频上传（未完成）
*   支持在表单提交页上传、删除和预览多个视频并在点击广告时随机播放
*   已完成demo，但仍有bug存在，来不及做了

## 3\. 接口定义 (API Specification)

| 方法 | 路径 | 描述 | 参数/备注 |
| :--- | :--- | :--- | :--- |
| `GET` | `/ads` | 获取广告列表 | 返回按 Score 排序后的列表 |
| `POST` | `/ads` | 创建新广告 | Body: `{ title, video, ... }` |
| `PUT` | `/ads/:id` | 编辑广告 | |
| `DELETE`| `/ads/:id` | 删除广告 | 触发智能文件删除逻辑 |
| `POST` | `/ads/:id/click` | 增加点击量 | 热度 +1 |
| `POST` | `/ads/upload` | 上传文件 | `Content-Type: multipart/form-data` |
| `GET` | `/ads/schema` | 获取表单配置 | 返回 JSON Schema |

## 4\. Git Commit记录
- chore: 完成前端初始化
- feat: 完成脚手架与基础架构搭建
- feat: 完成状态管理和排序算法
- feat: 完成UI组件开发
- feat: 完成表单开发
- feat: 完成页面整合
- refactor: 分离臃肿的索引文件功能
- chore: 初始化后端及数据库
- docs: 新增临时README和开发文档
- docs: 修正了文档
- feat: 完成CRUD接口
- feat: 整合前后端，并用react query代替zustand作服务端状态管理...
- refactor: 扩展koa类型包含body
- refactor: 前端不再计算分数，仅展示后端排序好的内容
- refactor: 拆分前端types索引，仅作导出
- feat: 新增视频的上传预览和本地储存
- refactor: 调整了AdModal的样式
- feat: 完成了视频播放器，现在点击广告将播放视频
- feat: 视频播放后不跳转，而是打开结算层
- refactor: 分离VideoModal
- refactor: 使用react query管理isUploading状态
- feat: 动态表单
- fix: 修复了缺失的视频文件删除
- refactor: 将后端app应用配置和index启动入口分离
- feat: 使用dotenv管理配置
- fix: 修复了广告在删除时会删除掉多个广告共用的视频源的问题

## 开发问题分析

### 竞价排名逻辑迁移 (架构变更)
开始实现前后端分离，前端不再计算分数，仅渲染展示后端排序好的内容。拟在后端controller中实现该业务逻辑

### 状态管理库迁移：
增加服务端后，前端不应再保留业务数据，只需要接受并渲染来自后端的数据。所以状态管理库从管理客户端状态的`zustand`改为了管理服务端状态（缓存）的`react query`并重构

### koa类型扩展
在 TypeScript 中使用 Koa 时，因为 Koa 的核心库并不包含 body 解析功能（通常由 koa-bodyparser 或 koa-body 提供），所以默认的类型定义中 ctx.request.body 是不存在的。

运行时（JavaScript）：koa-bodyparser 会在 ctx.request 上添加 body<br>
编译时（TypeScript）：它看不到这个属性 → 报错

所以用 类型断言解决：

- 方法 1：直接用any（曾用）
`const body = (ctx.request as any).body;`
- 方法 2：给 request 声明更准确的类型
- 方法 3：扩展 Koa 的类型（现用）
```
declare module "koa" {
  interface Request {
    body: any;
  }
}
```

### Barrel Pattern
使用index文件统一导出同一个文件夹下的内容，简化了其他模块的导入。在开发过程中因划分不明确多次将业务逻辑/UI渲染写入，最后均已分离

### UI+逻辑
将每个组件中的`Component.tsx`拆分为`Component.tsx`（渲染UI）和`useComponent.ts`（业务逻辑）

### 后端数据库选型
本打算用LowDB直接读写JSON文件作为轻量级存储方案，但最后选用了SQLite+Prisma的方案，原因主要有
- 类型安全: 结合Prisma ORM，可以通过schema.prisma定义数据模型，并自动生成TypeScript类型。消除了数据库SQL字段与TS接口代码不一致的隐患。
- 可维护性与迁移: 随着业务发展（如新增“多视频支持”），Prisma Migrate 提供了标准化的数据库迁移流程，能够安全地变更表结构。相比之下，维护JSON文件的结构需要手动编写脚本读取。并且prisma支持更方便地换用其他规模更大更复杂的数据库，而LowDB与SQL数据库完全不兼容

### 视频上传
不同于其他普通表单字段，视频文件是二进制流，数据庞大，不能直接存在数据库里。需要将文件和URL字段分开存储
#### 阶段 1：接收与物理落盘 (Backend)
1. 前端通过 `multipart/form-data` 格式将视频文件发送给后端。
2. 后端使用中间件（`@koa/multer`）拦截请求。
3. 中间件自动将文件流写入到服务器的 `server/uploads` 文件夹中。
4. 为了防止文件名冲突（比如两个用户都传了 `test.mp4`），我们会重命名文件，通常使用 UUID（如 `f47ac10b-58cc...mp4`）。
#### 阶段 2：生成访问凭证 (Backend)
1. 文件保存成功后，后端知道文件名是 `f47ac10b...mp4`。
2. 后端拼接出一个 HTTP URL，例如：`http://localhost:3000/uploads/f47ac10b...mp4`。
3. 后端将这个 URL 返回给前端。
#### 阶段 3：业务关联 (Database)
1. 前端拿到 URL 后，将其填入“创建广告”表单的 `video` 字段。
2. 提交表单时，数据库的 `Ad` 表中，`video` 列存的就是这串 URL 字符。

### 浏览器阻止弹窗
- 因为从用户点击卡片开始，到视频播放结束，中间隔了几秒甚至几十秒，浏览器认为这个 open 动作不再是由用户直接触发的，所以拦截了它。
- 作为**解决方案**，视频播放后不再转跳落地页，而且弹出结算层供用户自行前往

### 视频播放交互流程更新 (加入结算层)
- 点击广告卡片 -> 打开视频弹窗(模态框)播放。
- 播放结束 -> 不直接跳转，而是展示结算层。(暂停并覆盖一层半透明遮罩)
- 用户决策：结算层提供重播和立即查看（跳转落地页）两个按钮。 这种设计不仅解决了浏览器拦截window.open的问题，还优化了用户体验。

### 现有的Loading标志
不再手动使用`useState`，而是用`react query`
- 列表加载 AdList管理，由useQuery提供isLoading标志
- 表单提交 AdList管理，useMutation自动提供isPending，由isSubmitting组合
- 视频上传 AdModal管理，useMutation提供upload.loading

### 动态表单
1. Schema定义 (Server): 后端在`server/src/config/formSchema.ts`中定义 JSON 结构，描述字段的元数据（类型、标签、校验规则、默认值等）。
2. Schema下发 (API): 提供`GET /ads/schema`接口，将配置发送给前端。
3. 组件映射 (Client): 前端`AdModal`组件不写死`<Input />`，而是遍历 Schema数组，通过switch(item.component)动态匹配Ant Design的组件（Input, TextArea, VideoUpload 等）。
4. 校验注入: 后端的rules配置直接透传给Antd Form的rules属性，实现了校验逻辑的动态化。
- 后端`src/config/formSchema`决定了表单是什么样，前端`AdFormData`决定了数据是什么样

#### 问题举例
- 我们想要自己控制Upload的网络请求（使用`Axios`和`React Query`）但是`Ant design`的`<Upload />`组件会自己发起AJAX请求，而且没有直接导出`RcCustomRequestOptions`，所以需要自己从`customRequest`导出所需要的属性。

### 后端配置管理 (Dotenv)
- 引入了dotenv管理环境变量（如端口、数据库地址、上传路径）。方便基础变量的改写，同时保护机密信息。
- 新增了env.ts统一收敛配置读取逻辑，避免在代码中到处使用process.env。