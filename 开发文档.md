# 开发文档

## 1\. 架构设计

### 1.1 前端设计模式

为了解决复杂业务逻辑与 UI 耦合的问题，项目严格遵循 **View + Hook** 的分离模式：

  * **UI 层 (`index.tsx` / `Component.tsx`)**: 仅负责视图渲染和样式布局，不包含业务状态逻辑。
  * **逻辑层 (`useHook.ts`)**: 封装状态（State）、副作用（Effect）和交互处理函数（Handlers），向 UI 层暴露接口。
  * **入口文件 (`index.ts`)**: 采用 Re-export 模式转发组件，保持引用路径简洁。

### 1.2 后端分层

后端采用经典的 MVC 变体结构：

  * **Router**: 定义 API 路径与 HTTP 方法。
  * **Controller**: 处理请求参数，调用 Prisma 进行数据库操作，返回响应。
  * **Prisma Client**: 自动生成的强类型数据库访问层。

## 2\. 核心逻辑实现

### 2.1 竞价排名算法

广告列表不按时间排序，而是依据“热度”和“出价”综合计算权重。

**公式**：
$$Score = Pricing + (Pricing \times Clicked \times 0.42)$$

  * `Pricing`: 广告主出价。
  * `Clicked`: 广告被点击次数。
  * `0.42`: 调节系数（神秘系数）。

**实现位置**: `client/src/utils/ranking.ts`
该逻辑在前端 Store 更新及后端查询接口（规划中）中均需保证一致。

### 2.2 动态表单 (进阶规划)

针对进阶任务3，表单渲染将由硬编码改为 Schema 驱动。

  * 后端定义 JSON Schema 描述字段类型、校验规则。
  * 前端开发 `SchemaForm` 组件，递归解析 Schema 并动态生成 Ant Design Form Item。

## 3\. 接口定义 (API Specification)

> 目前后端处于开发阶段，规划以下核心接口：

| 方法 | 路径 | 描述 | 参数示例 |
| :--- | :--- | :--- | :--- |
| `GET` | `/ads` | 获取广告列表（支持排序） | - |
| `POST` | `/ads` | 创建新广告 | `{ title, content, pricing, ... }` |
| `PUT` | `/ads/:id` | 编辑广告 | `{ title, pricing, ... }` |
| `DELETE`| `/ads/:id` | 删除广告 | - |
| `POST` | `/ads/:id/click` | 增加广告点击量 | - |

## 4\. 常见问题 (FAQ)

  * **Q: 为什么后端启动报错 `Prisma Client not found`?**
      * A: 请确保在 `server` 目录下执行了 `npx prisma generate` 或 `npx prisma migrate dev`。
  * **Q: 为什么修改了 `.ts` 文件没有自动重启?**
      * A: 确保使用的是 `npm run dev`，底层依赖 `tsx watch` 监听文件变动。

## 开发问题分析
### koa类型扩展
在 TypeScript 中使用 Koa 时，因为 Koa 的核心库并不包含 body 解析功能（通常由 koa-bodyparser 或 koa-body 提供），所以默认的类型定义中 ctx.request.body 是不存在的。

运行时（JavaScript）：koa-bodyparser 会在 ctx.request 上添加 body<br>
编译时（TypeScript）：它看不到这个属性 → 报错

所以用 类型断言解决：

- 方法 1：
`const body = (ctx.request as any).body;`

- 方法 2：给 request 声明更准确的类型（更干净的写法）
```
interface RequestBody {
  body: any;
}
const request = ctx.request as RequestBody;
const body = request.body;
```

- 方法 3：扩展 Koa 的类型（最佳实践）
```
declare module "koa" {
  interface Request {
    body: any;
  }
}
```

### Barrel Pattern
使用index文件统一导出同一个文件夹下的内容，简化了其他模块的导入

### 视频上传
不同于其他普通表单字段，视频文件是二进制流，数据庞大，不能直接存在数据库里。需要将文件和URL字段分开存储
#### 阶段 1：接收与物理落盘 (Backend)
1. 前端通过 `multipart/form-data` 格式将视频文件发送给后端。
2. 后端使用中间件（`@koa/multer`）拦截请求。
3. 中间件自动将文件流写入到服务器的 `server/uploads` 文件夹中。
4. 为了防止文件名冲突（比如两个用户都传了 `test.mp4`），我们会重命名文件，通常使用 UUID（如 `f47ac10b-58cc...mp4`）。
#### 阶段 2：生成访问凭证 (Backend)
1. 文件保存成功后，后端知道文件名是 `f47ac10b...mp4`。
2. 后端拼接出一个 HTTP URL，例如：`http://localhost:3000/uploads/f47ac10b...mp4`。
3. 后端将这个 URL 返回给前端。
#### 阶段 3：业务关联 (Database)
1. 前端拿到 URL 后，将其填入“创建广告”表单的 `video` 字段。
2. 提交表单时，数据库的 `Ad` 表中，`video` 列存的就是这串 URL 字符。

### 浏览器阻止弹窗
因为从用户点击卡片开始，到视频播放结束，中间隔了几秒甚至几十秒，浏览器认为这个 open 动作不再是由用户直接触发的，所以拦截了它。